[package]
name = "loom-domain"
description = "loom domain model: projects, threads, hosts and environments as pure types and invariants"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true
