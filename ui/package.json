{
  "name": "@loom/ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev-server.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.reference.config.ts --passWithNoTests"
  },
  "dependencies": {
    "@bb/domain": "workspace:*",
    "@bb/thread-view": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
