# Rust
/target
**/target

# Node (bb fork apps live here later)
node_modules/
**/node_modules/
dist/
**/dist/

# Vite's bundle report for the app's budget check: generated by every build,
# read by `check:bundle`, never committed.
apps/app/bundle-stats.json

# Local state
*.log
.DS_Store
.bb-dev/
