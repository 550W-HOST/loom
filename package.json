{
  "name": "loom-ui-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm --filter @loom/ui run build && pnpm --filter './ui/packages/*' run build && pnpm --filter @loom/product-app run build",
    "typecheck": "pnpm --filter @loom/ui run typecheck && pnpm --filter './ui/packages/*' run typecheck && pnpm --filter @loom/product-app run typecheck",
    "test": "pnpm --filter @loom/ui run test && pnpm --filter './ui/packages/*' run test && pnpm --filter @loom/product-app run test",
    "ui:boundary": "pnpm --filter @loom/product-app run check:boundary",
    "ui:browser": "pnpm --filter @loom/product-app run test:browser",
    "provenance:check": "node scripts/check-ui-provenance.mjs",
    "example": "tsx ui/examples/thread-timeline.ts"
  },
  "dependencies": {
    "@bb/domain": "workspace:*",
    "@bb/thread-view": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
