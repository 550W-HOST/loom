{
  "name": "loom-ui-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm --filter './ui/packages/*' run build && pnpm --filter @bb/app run build",
    "typecheck": "pnpm --filter './ui/packages/*' run typecheck && pnpm --filter @bb/app run typecheck",
    "test": "pnpm --filter './ui/packages/*' run test && pnpm --filter @bb/app run test",
    "check:bundle": "pnpm --filter @bb/app run check:bundle"
  },
  "dependencies": {
    "@bb/domain": "workspace:*",
    "@bb/thread-view": "workspace:*"
  },
  "devDependencies": {
    "@bb/tsconfig": "workspace:*",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
