{
  "name": "loom-ui-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm --filter './ui/packages/*' run build",
    "typecheck": "pnpm --filter './ui/packages/*' run typecheck",
    "test": "pnpm --filter './ui/packages/*' run test",
    "example": "tsx ui/examples/thread-timeline.ts"
  },
  "dependencies": {
    "@bb/domain": "workspace:*",
    "@bb/thread-view": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
