{
  "name": "loom-ui-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm --filter @loom/ui run build && pnpm --filter './ui/packages/*' run build",
    "typecheck": "pnpm --filter @loom/ui run typecheck && pnpm --filter './ui/packages/*' run typecheck",
    "test": "pnpm --filter @loom/ui run test && pnpm --filter './ui/packages/*' run test",
    "provenance:check": "node scripts/check-ui-provenance.mjs",
    "port-plan:check": "node scripts/analyze-ui-port.mjs",
    "port-plan:write": "node scripts/analyze-ui-port.mjs --write",
    "port-plan:test": "node --test scripts/analyze-ui-port.test.mjs",
    "example": "tsx ui/examples/thread-timeline.ts"
  },
  "dependencies": {
    "@bb/domain": "workspace:*",
    "@bb/thread-view": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
